[project]
name = "os-sandbox"
version = "0.2.0"
description = "Axon code execution sandbox service (Modal)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "modal>=0.73.0",
]

[tool.setuptools.packages.find]
include = ["axon_sandbox*"]
